# Test tools for developers and reviewers - not installed with the application.
# The UI tests also need a browser: venv\Scripts\python.exe -m playwright install chromium
pytest==9.1.1
pytest-xdist==3.8.0
playwright==1.62.0
